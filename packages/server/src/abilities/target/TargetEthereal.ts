import BaseTargetAbility from './BaseTargetAbility'

export default class TargetEthereal extends BaseTargetAbility {
  static displayName = 'Target Ethereal'
  static description = 'Ignore modifiers to save (positive or negative) when making save rolls'
}
